name: meetgrow
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    # Agent API
    - openai>=1.30.0
    # OCR
    - paddlepaddle>=2.6.0
    - paddleocr>=2.8.0
    # ASR
    - funasr>=1.1.0
    - torchaudio>=2.1.0
    - torch>=2.1.0,<2.5.0
    # TTS
    - edge-tts>=6.1.0
    - aiohttp>=3.9.0
    # Documents
    - python-docx>=1.1.0
    - openpyxl>=3.1.0
    - pillow>=10.2.0
    # Utils
    - pydantic>=2.6.0
    - pyyaml>=6.0.1
    - rich>=13.7.0
    - click>=8.1.7
    # Dev
    - pytest>=8.0.0
